FROM openjdk:8-jdk-alpine
VOLUME /tmp
ARG JAR_FILE
COPY ${JAR_FILE} app.jar
ENV BROKER_HOST_NAME=localhost
ENV MONGO_HOST_NAME=localhost
ENTRYPOINT java -jar /app.jar $BROKER_HOST_NAME $MONGO_HOST_NAME